# 初始化状态操作逻辑与测试矩阵

## 目标

保证用户第一次打开小程序、没有真实云数据、只有创建者本人档案、没有药品、没有病程记录时，每一个入口都有明确的下一步，不出现空选择器、无效保存、静默失败或误以为已经记录成功。

当前开发测试版本默认开启 `ENABLE_DEV_MOCK_LOGIN`。在微信开发者工具中启动时，会跳过 `wx.login()` 和云函数，直接使用本地测试身份完成登录：

- `openid`: `devtools-openid`
- `nickname`: `测试用户`
- `currentFamilyId`: `demo-family-001`
- `loginMode`: `test`

该开关只对 `envVersion=develop` 生效，体验版和正式版会强制走真实登录。需要在开发者工具中联调真实登录时，把 `miniprogram/app.js` 顶部的 `ENABLE_DEV_MOCK_LOGIN` 临时改为 `false`，并配置云环境。

## 全局状态机

1. 未启动
2. 启动中：识别当前小程序版本
3. 登录中：
   - 开发版 Mock 模式：跳过微信接口，落到测试用户
   - 真实登录模式：调用 `wx.login()` 和 `login` 云函数，创建或读取用户
4. 已登录：
   - 允许读取首页、个人中心、家庭空间、会员、药箱、健康记录
   - 所有 `api` 调用仍会二次确认登录，防止页面漏判
5. 登录失败：
   - 页面弹窗提示“需要微信登录”
   - 用户可点“重新登录”

## 初始化数据规则

后端当前会自动创建默认家庭空间，因此真实初始化通常是：

- 有用户
- 有默认家庭
- 有 1 位自动创建并关联 owner 账号的“本人”家庭成员
- 没有药品
- 没有生病记录
- 没有用药记录

产品逻辑仍保留“无家庭空间”的兜底判断，避免历史脏数据或后续多家庭能力调整后出现断路。

## 操作门禁

| 功能入口 | 必要条件 | 不满足时的处理 |
| --- | --- | --- |
| 个人中心首页 | 已登录 | 弹窗要求微信登录 |
| 查看/修改个人信息 | 已登录 | 弹窗要求微信登录 |
| 添加家庭成员 | 已登录 + 有家庭空间 | 未登录先登录；无家庭去个人中心 |
| 记一次生病 | 已登录 + 有家庭空间 + 至少 1 个家庭成员 | 弹窗提示先添加家庭成员，确认后跳个人中心并自动打开添加成员弹窗 |
| 记录一次用药 | 已登录 + 有家庭空间 + 至少 1 个成员 + 至少 1 个药品 | 缺成员先引导添加成员；有成员但缺药品则跳转到独立的添加药品页 |
| 从病程详情记录用药 | 同“记录一次用药” | 满足后直接打开用药二级表单并带上病程和成员；不满足则先补基础资料 |
| 药箱添加药品 | 已登录 + 有家庭空间 | 成员可选，默认全家通用 |
| AI 助手 | 已登录 | 未登录先登录；可在空数据下回答“暂无记录”类内容 |
| 导出报告 | 已登录 | 未登录先登录；空数据生成空摘要或提示无记录 |
| 会员中心/兑换会员码 | 已登录 + 有家庭空间 | 未登录先登录；会员权益归属当前家庭 |
| 家庭邀请/切换 | 已登录 | 未登录先登录 |
| 创建家庭 | 已登录 + 未达到账号创建额度 | 默认家庭计入额度；免费账号最多 1 个，有效会员账号最多 3 个；超限时引导开通会员或提示已达上限 |
| 图片解析 | 已登录 + 已上传图片 | 未上传图片提示先上传 |

## 推荐的新用户路径

1. 第一次打开首页：自动完成微信/测试登录。
2. 用户可以直接为“本人”记录一次生病。
3. 如果要记录孩子、老人等其他家人，先进入家庭管理添加成员。
4. 保存其他成员后，可以从成员列表发起本人邀请并选择查看者、协作者或管理员权限。
5. 用户点“记录一次用药”：
   - 没有成员：先添加成员
   - 有成员但没有药品：跳药箱并打开独立的添加药品页
6. 用户保存药品后，从用药列表进入二级表单，记录剂量、时间和反应并扣库存。

## 测试矩阵

| 编号 | 初始条件 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| T01 | 开发工具 + `ENABLE_DEV_MOCK_LOGIN=true` | 启动小程序 | 不调用微信登录和云函数；`loginStatus=success`，`loginMode=test`，可进入首页 |
| T02 | 已登录，无成员 | 首页点“记一次生病” | 弹窗提示先添加家庭成员 |
| T03 | T02 后点“添加成员” | 跳个人中心 | 自动打开添加成员弹窗 |
| T04 | 已登录，无成员 | 健康页点“记一次生病” | 不展开表单，提示添加成员 |
| T05 | 已登录，有成员，无药品 | 用药页点“记录一次用药” | 弹窗提示先添加药品，确认后跳药箱并打开独立添加页 |
| T06 | 已登录，无成员、无药品 | 用药页点“记录一次用药” | 先提示添加家庭成员 |
| T07 | 已登录，有成员、有药品、无病程 | 从用药列表进入二级表单记录用药 | 可保存，病程关联为可选 |
| T08 | 已登录，有病程、无药品 | 病程详情点“记录用药” | 提示先添加药品 |
| T09 | 未登录或登录失败 | 个人中心点个人信息 | 弹窗要求微信登录 |
| T10 | 已登录 | 个人中心点个人信息 | 可查看并保存昵称、头像、电话、邮箱、备注 |
| T11 | 已登录，无健康数据 | AI 助手提问 | 不报错，返回基于空记录的回答 |
| T12 | 已登录，无健康数据 | 导出报告 | 不报错，报告中体现暂无记录 |
| T13 | 已登录 | 输入已有会员兑换码 | 成功时会员归属当前家庭，失败时显示具体错误 |
| T14 | 已登录，未上传图片 | 图片解析页点整理 | 提示先上传图片 |
| T15 | 免费账号已有默认家庭 | 进入家庭管理 | 显示已创建 1/1，创建入口改为会员升级引导 |
| T16 | 当前账号自己创建的任一家庭为有效会员，且共拥有 1 或 2 个家庭 | 创建新家庭 | 创建成功、自动切换，新家庭仅有创建者本人档案且默认为免费版 |
| T17 | 当前账号已拥有 3 个家庭 | 再次创建 | 页面和后端均拒绝，已有家庭仍可正常切换 |

## 开发实现原则

- 页面层做用户体验门禁：弹窗、跳转、自动打开表单。
- 服务层做安全兜底：所有 `api` 调用先确认登录。
- 后端继续做权限校验：家庭角色、记录归属、配额限制不能只靠前端。
- 初始化空态优先引导“下一步动作”，不要让用户面对空 picker 或保存失败才知道缺什么。
